<!--<p style="color: green"><%= notice %></p> -->

<% if user_signed_in? %>
    <table class="table table-striped table-bordered table-hover">
      <thead>
        <tr class="table-dark">
          <th>Name</th>
          <th>Email</th>
          <th>Phone</th>
          <th>Twitter</th>
          <th colspan="1"></th>
          
        </tr>
      </thead>
      
      <tbody>
        <% @friends.each do |friend| %>
          <% if friend.user == current_user %>
          <tr>
            <td><%= link_to friend.first_name + ' ' + friend.last_name, friend %> </td>
            <td><%= friend.email %></td>
            <td><%= friend.phone %></td>
            <td><%= friend.twitter %></td>
            
       
            <!--<td><%= button_to "Destroy", friend, method: :delete, form: {data: { "turbo_confirm": 'Are you sure?' }}, class: "btn btn-link align-top" %> </td>
            
            <td><%= button_to "Destroy", friend, method: :delete, data: { confirm: 'Are you sure?' }, data: { "turbo-method": "delete" }, class: "btn btn-link align-top" %></td>
            -->

            <td><%= link_to "Delete", friend, method: :delete, data: { turbo_method: :delete, turbo_confirm:"Are you sure?"}, class: "btn btn-outline-danger btn-sm" %></td>
            
          </tr>
            <% end %>
          <% end %>      
      </tbody>

    </table>


    <%= link_to "New friend", new_friend_path, class:"btn btn-secondary" %>
<% else %>
<h1> Hello World!</h1>
This is my 2nd rails webpage ...
<% end %>
